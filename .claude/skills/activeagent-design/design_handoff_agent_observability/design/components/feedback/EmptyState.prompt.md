Centered empty state per DESIGN_SPEC: "No [items] yet" + how to create the first one.

\`\`\`jsx
<EmptyState title="No traces yet" hint="Run an agent to see its first trace here" action={<Button>Run Agents</Button>} />
\`\`\`
