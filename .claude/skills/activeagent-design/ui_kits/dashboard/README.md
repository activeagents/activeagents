# Active Agent — Dashboard UI kit
Recreation of the product dashboard (the activeagent gem's `lib/active_agent/dashboard/frontend/components/dashboard/*`). Screens: Agents (fleet grid), Agent detail (tabs), Traces (waterfall), Metrics, Evaluations. Interactive: sidebar nav + dark/light toggle. Composes components/ primitives via the DS bundle.
