# Branch: codex/import-evaluation-reports

Base: `96696df3cb78e2259c669d5a9b17605a0f06f7f6` (`main`).

Scope: authenticated external evaluation import API; immutable account-scoped report storage; dashboard scenario/model result display; trace links and observed-agent guards. No framework dependency update is required by the wire protocol.

Issue: [external evaluation report ingestion](../issues/external-evaluation-reports.md).

PR: prepared locally for review; publication is coordinated separately.
