<section>
  <div class="heading hero centered">
    <h1 class="balanced">
      <span class="color-accent">Active Agent</span><br />
      Build AI in Rails
    </h1>
    <p class="paragraph l secondary balanced">
      Watch AI agents work. Take over anytime. Ship the Rails way.
    </p>
    <div class="button-group margin-paragraph centered">
      <%= link_to "Getting Started", "https://docs.activeagents.ai/getting-started", class: "button primary" %>
      <%= link_to "Docs", "https://docs.activeagents.ai", class: "button tertiary" %>
      <%= link_to "GitHub", "https://github.com/activeagents/activeagent", class: "button tertiary" %>
    </div>
  </div>

  <!-- Session Replay Hero - Interactive Demo -->
  <div class="session-replay-hero"
       data-controller="session-replay"
       data-session-replay-handoff-enabled-value="true"
       data-session-replay-handoff-step-value="4">

    <!-- Browser + Sidebar Row -->
    <div class="session-replay-hero-main">
      <div class="replay-browser-hero">
      <div class="browser-chrome">
        <div class="browser-dots">
          <span class="dot"></span><span class="dot"></span><span class="dot"></span>
        </div>
        <div class="browser-address">
          <i class="fa-solid fa-lock"></i>
          <span class="address-text">checkout.stripe.com/pay/cs_test_...</span>
        </div>
        <div class="cassette-badge" data-session-replay-target="cassetteBadge">
          <i class="fa-solid fa-circle recording"></i>
          <span>REC</span>
        </div>
      </div>

      <div class="browser-viewport-hero"
           data-session-replay-target="viewport"
           data-action="mouseenter->session-replay#pauseOnHover mouseleave->session-replay#resumeOnLeave">
        <div class="page-content checkout-page">
          <div class="checkout-form">
            <div class="form-section">
              <div class="form-label">Card number</div>
              <div class="form-input card-field">
                <span class="card-icon"><i class="fa-brands fa-cc-visa"></i></span>
                <input type="text" class="checkout-input"
                       data-session-replay-target="card"
                       data-action="input->session-replay#trackUserInput"
                       placeholder="4242 4242 4242 4242"
                       disabled>
              </div>
            </div>
            <div class="form-row">
              <div class="form-section half">
                <div class="form-label">Expiry</div>
                <div class="form-input expiry-field">
                  <input type="text" class="checkout-input"
                         data-session-replay-target="expiry"
                         data-action="input->session-replay#trackUserInput"
                         placeholder="MM/YY"
                         disabled>
                </div>
              </div>
              <div class="form-section half">
                <div class="form-label">CVC</div>
                <div class="form-input cvc-field">
                  <input type="text" class="checkout-input"
                         data-session-replay-target="cvc"
                         data-action="input->session-replay#trackUserInput"
                         placeholder="123"
                         disabled>
                </div>
              </div>
            </div>
            <button class="checkout-btn"
                    data-session-replay-target="pay"
                    data-action="click->session-replay#userPay"
                    disabled>Pay $99.00</button>
          </div>
        </div>

        <%# Agent Cursor %>
        <div class="agent-cursor at-card" data-session-replay-target="cursor">
          <svg class="cursor-icon" viewBox="0 0 24 24" fill="currentColor">
            <path d="M4 4l16 8-7 2-2 7z"/>
          </svg>
          <span class="cursor-label">Agent</span>
        </div>

        <%# Handoff Overlay %>
        <div class="handoff-overlay" data-session-replay-target="handoffOverlay" style="display: none;">
          <div class="handoff-prompt">
            <div class="handoff-icon">
              <i class="fa-solid fa-hand"></i>
            </div>
            <h3>Agent paused</h3>
            <p>Take over and complete the checkout yourself!</p>
            <button class="handoff-btn" data-action="click->session-replay#takeOver">
              <i class="fa-solid fa-play"></i> Take Over Session
            </button>
            <p class="handoff-hint">Your actions will be tracked in the trace log</p>
          </div>
        </div>
      </div>
    </div>

    <div class="replay-sidebar-hero">
      <div class="sidebar-header">
        <span class="cassette-icon"><i class="fa-solid fa-tape"></i></span>
        <span class="cassette-name">checkout_flow.vcr</span>
      </div>
      <div class="action-list" data-session-replay-target="actionList">
        <%# Actions populated by Stimulus controller %>
      </div>

      <%# Trace Log %>
      <div class="trace-log" data-session-replay-target="traceLog" style="display: none;">
        <div class="trace-log-header">
          <i class="fa-solid fa-wave-square"></i> Trace Log
        </div>
        <div class="trace-entries"></div>
      </div>
    </div>
    </div><!-- /.session-replay-hero-main -->

    <div class="replay-controls-hero">
      <div class="replay-timeline"
           data-session-replay-target="timeline"
           data-action="click->session-replay#seekTimeline">
        <div class="timeline-track">
          <div class="timeline-progress" data-session-replay-target="timelineProgress" style="width: 20%;"></div>
          <div class="timeline-snapshots">
            <span class="snapshot-marker completed" style="left: 5%;" title="Initial load"
                  data-action="click->session-replay#jumpToSnapshot" data-step="0"><i class="fa-solid fa-camera"></i></span>
            <span class="snapshot-marker" style="left: 50%;" title="Form filled"
                  data-action="click->session-replay#jumpToSnapshot" data-step="4"><i class="fa-solid fa-camera"></i></span>
            <span class="snapshot-marker pending" style="left: 95%;" title="Payment complete"
                  data-action="click->session-replay#jumpToSnapshot" data-step="6"><i class="fa-solid fa-camera"></i></span>
          </div>
          <div class="timeline-playhead" data-session-replay-target="playhead" style="left: 20%;"></div>
        </div>
      </div>
      <div class="replay-buttons">
        <button class="replay-btn" data-action="click->session-replay#stepBackward" title="Previous step">
          <i class="fa-solid fa-backward-step"></i>
        </button>
        <button class="replay-btn play-btn" data-session-replay-target="playPauseBtn" data-action="click->session-replay#togglePlayPause" title="Play/Pause">
          <i class="fa-solid fa-pause" data-session-replay-target="playPauseIcon"></i>
        </button>
        <button class="replay-btn" data-action="click->session-replay#stepForward" title="Next step">
          <i class="fa-solid fa-forward-step"></i>
        </button>
        <button class="replay-btn speed-btn" data-session-replay-target="speedBtn" data-action="click->session-replay#cycleSpeed" title="Playback speed">
          <span data-session-replay-target="speedDisplay">1x</span>
        </button>
        <button class="replay-btn restart-btn" data-action="click->session-replay#restart" title="Restart">
          <i class="fa-solid fa-rotate-left"></i>
        </button>
        <span class="replay-time" data-session-replay-target="stepCounter">Step 1/7</span>
      </div>
    </div>
  </div>

  <a href="#features" class="scroll-indicator">
    <span>See what's included</span>
    <i class="fa-solid fa-chevron-down chevron"></i>
  </a>
</section>
