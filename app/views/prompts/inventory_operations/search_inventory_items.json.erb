{
  {
    "get_inventory_item_by": {
      "description": "Retrieves an inventory item based on either the name, code, or nearest neighbor embedding.",
      "parameters": {
        "name": {
          "type": "string",
          "description": "The name of the inventory item to retrieve."
        },
        "code": {
          "type": "string",
          "description": "The code of the inventory item to retrieve."
        },
        "embedding": {
          "type": "array",
          "description": "The embedding vector to find the nearest inventory item.",
          "items": {
            "type": "number"
          }
        }
      }
    }
  }
}